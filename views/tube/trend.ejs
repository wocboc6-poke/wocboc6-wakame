<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>わかめtube</title>
  <meta name="description" content="まっしろになってゆく">
  <meta property="og:title" content="わかめtube">
  <meta property="og:description" content="まっしろになってゆく">
  <meta property="og:site_name" content="わかめtube | まっしろになっていく">
  <meta property="og:image" content="https://cdn.glitch.global/5b8b419f-e61e-4533-9e15-5b2805b88d0e/IMG_1111_Original.jpeg">
  <meta name="theme-color" content="#ff8181">
  <link rel="shortcut icon" href="https://cdn.glitch.global/5b8b419f-e61e-4533-9e15-5b2805b88d0e/IMG_1111_Original.jpeg">
  <link rel="icon" href="https://cdn.glitch.global/5b8b419f-e61e-4533-9e15-5b2805b88d0e/IMG_1111_Original.jpeg" type="image/x-icon">
  <meta name="author" content="わかめtube | まっしろ">
  <link rel="stylesheet" href="https://code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
  <script src="https://code.jquery.com/jquery-3.5.1.js"></script>
  <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
  <header>
    <div class="banner">
      <button class="btn btn-link" onclick="window.location.href='/wkt'">ホーム<br>🏠</button>
    </div>
  </header>

  <main class="container text-center">
    <div class="search-bar">
      <form action="/wkt/s" method="get" id="searchForm">
        <input type="text" name="q" id="searchbox" class="form-control" placeholder="キーワードを入力..." required>
        <button type="submit" class="btn btn-search">検索(けんさく)</button>
      </form>
    </div>
  </main>
  <br>
  
  <h2>ようこそ！</h2>
  <p>好きな動画を検索してみましょう！</p>
  
  <!-- ★★★ トレンド動画を表示する部分 ★★★ -->
  <div class="results" id="trend-container"></div>

  <br><br>
  <footer class="text-center">
    <a href="/app"><span class="mt-5 mb-3 text-muted">app🎮</span></a><br><br><br>
    <a href="/wakams"><span class="mt-5 mb-3 text-muted">わかめMusic🎶</span></a><br><br><br>
    <a href="/tools"><span class="mt-5 mb-3 text-muted">ツール🧰</span></a><br><br><br>
  </footer>
<br><br>

<style>
#trend-container {
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
}

/* 1アイテムの枠 */
.result-item {
    width: 280px; /* ←サムネ大きめ */
}

/* サムネイルを大きめに */
.thumb {
    width: 100%;
    border-radius: 10px;
}

/* チャンネルアイコン行 */
.channel-row {
    display: flex;
    align-items: center;
    margin-top: 6px;
}

/* アイコン画像 */
.channel-icon {
    width: 26px;
    height: 26px;
    border-radius: 50%;
    margin-right: 8px;
}

/* ✔すべて統一フォントサイズ */
.channel-name,
.video-title,
.video-meta {
    font-size: 12px;
    line-height: 1.4em;
    color: #eee;
    text-decoration: none;
}

/* タイトルをわずかに強調 */
.video-title {
    font-weight: 600;
    margin-top: 4px;
}

/* メタ情報（再生回数など） */
.video-meta {
    color: #aaa;
    margin-top: 4px;
}
</style>

<div id="trend-container"></div>

<script>
async function loadTrends() {
    const url = "https://raw.githubusercontent.com/siawaseok3/wakame/refs/heads/master/trend.json";

    try {
        const res = await fetch(url);
        const json = await res.json();
        const trending = json.trending;

        const container = document.getElementById("trend-container");
        container.innerHTML = "";

        trending.forEach(item => {
            const videoId = item.id;
            const title = item.title;
            const channel = item.channel;
            const channelId = item.channelId;
            const icon = item.channelIcon || item.channelThumbnail;  // どちらでも対応
            const thumbnail = item.thumbnails.high.url;

            const html = `
                <div class="result-item">
                    <a href="/wkt/w/${videoId}">
                        <img loading="lazy" class="thumb" src="${thumbnail}" alt="${title}">
                    </a>

                    <div class="channel-row">
                        <img class="channel-icon" src="${icon}">
                        <a href="/wkt/c/${channelId}" class="channel-name">${channel}</a>
                    </div>

                    <p class="video-title">${title}</p>

                    <p class="video-meta">
                        ${Number(item.viewCount).toLocaleString()} 回再生 ・
                        ${Number(item.likeCount).toLocaleString()} 高評価 ・
                        ${Number(item.commentCount).toLocaleString()} コメント
                    </p>
                </div>
            `;
            container.innerHTML += html;
        });

    } catch (err) {
        console.error("Trend 読み込み失敗:", err);
    }
}

loadTrends();
</script>

<script>
/* オートコンプリート（元コードそのまま） */
$(function () {
    $('#searchbox').autocomplete({
        source: function (request, response) {
            $.ajax({
                url: '/wkt/back/suggest',
                data: { keyword: request.term },
                success: function (data) {
                    response(data);
                }
            });
        },
        delay: 1,
        select: function (event, ui) {
            $('#searchbox').val(ui.item.value);
            $('form').submit();
        }
    });
});
</script>

</body>
</html>
